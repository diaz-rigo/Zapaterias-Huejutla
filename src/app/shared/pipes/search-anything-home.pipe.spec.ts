import { SearchAnythingHomePipe } from './search-anything-home.pipe';

describe('SearchAnythingHomePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchAnythingHomePipe();
    expect(pipe).toBeTruthy();
  });
});
