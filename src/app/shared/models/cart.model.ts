export class CartItem {
  id!: string; // Este será el ID del producto
  variantId!: string; // Este será el ID de la variante
  name!: string;
  precio!: number;
  cantidad!: number;
  image!: string;
  size!: number; // Talla del zapato
  color!: string; // Color del zapato
}
